import React from 'react';

// This will import the existing PublicPortfolio content as the Creative template
const CreativeTemplate = ({ portfolio, user, isDark }) => {
  // Use the existing PublicPortfolio design which is already creative
  return null; // This will be handled in PublicPortfolio.jsx
};

export default CreativeTemplate;
